import { YearLabelPipe } from './year-label.pipe';

describe('YearLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new YearLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
